/*
 * Use Protocol Buffer version 3.
 */ 
syntax = "proto3";

/*
 * This is the data structure that carriers file name and its content.
 */ 
message FileMessage
{
    string name   = 1;
    bytes content = 2;
}
