/*
 * Use Protocol Buffer version 3.
 */ 
syntax = "proto3";

/*
 * This data type carries information about sender of the message.
 * Message itself is signed using sender's private key.
 */
message SourceMessage
{
    string sourceId = 1;
    bytes signedMsg = 2;
}
